# Usage

Launch all tests:

```console
$ ./tests.sh --all
```

Launch only non-interpret tests:

```console
$ ./tests.sh --noic
```

Launch only interpret tests:

```console
$ ./tests.sh --ic
```

Launch with valgrind:

```console
$ ./tests.sh --all --valgrind
```